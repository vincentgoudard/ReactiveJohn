import { Meteor } from 'meteor/meteor';
import '../imports/api/toggle_collection.js';

Meteor.startup(() => {
  // code to run on server at startup
  // if (ToggleCollection) ToggleCollection.remove({});
});

